apiVersion: 1

datasources:
  name: Prometheus
  type: prometheus
  uid: prometheus
  access: proxy
  url: https://cloud-service-prometheus.onrender.com
  isDefault: true
  editable: true
  jsonData:
    timeInterval: '15s'